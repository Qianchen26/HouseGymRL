# HouseGym RL 系统完整说明文档

## 一、系统概述

### 1.1 核心理念
本系统是一个**灾后房屋重建调度**的强化学习环境。核心思想是：

- **目标**：学习一个**鲁棒**的调度策略，而非最优策略
- **价值主张**：传统方法（LJF/SJF）各有失效模式，RL应该学会"永远不太差"的自适应策略
- **设计原则**：诚实测试，不预设偏见，关注泛化能力

### 1.2 问题设定
- **输入**：N栋受损房屋，每栋需要不同的重建时间
- **资源**：K个施工队（有限资源）
- **约束**：每天只能看到M=512个候选房屋（认知限制）
- **目标**：最小化总体完成时间，同时保持各种条件下的鲁棒性

## 二、环境架构

### 2.1 类层次结构

```python
BaseEnv (基类)
├── RLEnv (强化学习环境)
├── BaselineEnv (基线策略环境)
└── OracleEnv (Oracle环境，无M限制)
```

所有环境共享相同的约束和机制，确保公平比较。

### 2.2 核心数据结构

每个环境维护以下核心数组：
```python
self._arr_total    # 每栋房屋的总工作量（人天）
self._arr_rem      # 每栋房屋的剩余工作量
self._arr_dmg      # 损坏等级 (0=轻微, 1=中等, 2=严重)
self._arr_cmax     # 每栋房屋的最大日工人数
```

### 2.3 状态空间（观察）

观察向量维度：6 + 512×4 = 2054维

**全局特征（6维）**：
1. 当前天数 / 最大天数
2. 当前可用施工队数量 K
3. 等待队列大小
4. 已揭示房屋数量
5. 已揭示房屋的剩余工作比例
6. 已揭示房屋中严重损坏的比例

**候选特征（512×4维）**：
对于每个候选房屋：
1. 剩余工作量
2. 损坏等级
3. 最大日工人数
4. 有效掩码（1=有效，0=填充）

### 2.4 动作空间

连续动作空间：[0,1]^512

- RL为每个候选房屋输出一个分数
- 按分数排序，贪心分配资源
- 基线策略内部处理，不需要动作输入

## 三、关键机制

### 3.1 纯随机候选选择（最重要！）

```python
def _select_candidates(self, queue_ids: List[int]) -> List[int]:
    if len(queue_ids) <= self.M:
        return queue_ids

    # 关键：使用基于天数的种子，确保信息公平
    selection_seed = self.seed + self.day * 1000
    selection_rng = np.random.default_rng(selection_seed)
    selected = selection_rng.choice(queue_ids, size=self.M, replace=False)
    return selected.tolist()
```

**设计理由**：
- **无偏见**：不预设任何优先级（不像之前的40-40-20混合策略）
- **信息公平**：同一天所有方法看到相同的候选（通过day-based seed）
- **真实测试**：RL必须从自然样本中学习识别重要任务
- **鲁棒性测试**：当候选质量无法保证时的表现

### 3.2 批次到达系统

房屋分三批逐步揭示，模拟真实的损害评估过程：

```python
批次1（第0天）：  主要是严重损坏（紧急评估）
批次2（第21天）： 主要是中等损坏（详细评估）
批次3（第36天）： 主要是轻微损坏（完整评估）
```

配置：
```python
"batch_arrival": {
    "days": [0, 21, 36],
    "ratios": [0.40, 0.35, 0.25],
    "damage_priority": [2, 1, 0],  # 严重→中等→轻微
}
```

**现实依据**：基于2018年龙目岛地震，损害评估确实需要数周完成。

### 3.3 容量爬坡系统

施工队逐步动员，而非立即满负荷：

```python
阶段1（0-36天）：   K = 0（规划阶段，无施工）
阶段2（36-216天）： K从0线性增长到最大值
阶段3（216天后）：  K = 最大值（满负荷）
```

容量计算：
```python
def get_capacity(self, day: int) -> int:
    if day < 36:
        return 0
    elif day < 216:
        progress = (day - 36) / 180
        return int(self.max_K * progress)
    else:
        return self.max_K
```

**现实依据**：承包商需要时间动员，龙目岛震后约7个月才达到峰值容量。

### 3.4 跨可用性实验

测试不同资源稀缺程度下的表现：

```python
CREW_AVAILABILITY_LEVELS = [0.1, 0.3, 0.5, 0.7, 0.9, 1.0]
```

- **可用性1.0**：资源充足，预期RL优于LJF约7%
- **可用性0.5**：资源中等，预期RL优于LJF约15%
- **可用性0.1**：资源稀缺，预期RL优于LJF约25%

**假设**：RL的优势随资源稀缺性增加而增大。

## 四、基线策略

### 4.1 实现的策略

1. **LJF (Longest Job First)**
   ```python
   sorted_candidates = sorted(candidates, key=lambda h: -self._arr_total[h])
   ```
   优先处理总工作量最大的房屋

2. **SJF (Shortest Job First)**
   ```python
   sorted_candidates = sorted(candidates, key=lambda h: self._arr_total[h])
   ```
   优先处理总工作量最小的房屋

3. **Random**
   ```python
   random.shuffle(candidates)
   ```
   随机顺序处理

4. **Oracle-LJF/SJF**
   - 无M=512限制，能看到整个队列
   - 用于量化信息限制的成本（约165%的makespan惩罚）

### 4.2 失效模式分析

- **LJF**：资源充足时好，资源稀缺时差（大任务阻塞小任务）
- **SJF**：资源稀缺时好，资源充足时差（延迟关键路径）
- **Random**：平均表现，无灾难性失败，但也不优秀

**RL的价值**：学习自适应策略，根据当前状态选择类LJF或类SJF行为。

## 五、训练流程

### 5.1 数据来源

**训练**：合成数据
```python
def make_synth_env(H_min=10000, H_max=100000, worker_ratio=(0.1, 0.25)):
    # 随机生成房屋数量H
    # 随机生成施工队比例
    # 使用Dirichlet分布生成损坏分布
```

**测试**：真实数据（7个龙目岛地区）
- Mataram、West Lombok、North Lombok等
- 每个地区有真实的损坏分布和施工队数量

### 5.2 训练配置

```python
# 快速测试模式
if QUICK_TEST:
    N_ENVS = 2           # 2个并行环境
    TOTAL_STEPS = 1000   # 1000步（1-2回合）
else:
    N_ENVS = 10          # 10个并行环境
    TOTAL_STEPS = 300000 # 30万步
```

### 5.3 网络架构

由于观察空间增大到2054维：
```python
policy_kwargs = dict(
    net_arch=dict(
        pi=[512, 512],  # Actor网络
        qf=[512, 512]   # Critic网络
    )
)
```

### 5.4 奖励函数

简单的完成度增量奖励：
```python
reward = completion_t - completion_{t-1}
```

其中completion是已完成工作的比例。

## 六、评估体系

### 6.1 评估指标

1. **Makespan**：达到99%完成的天数
2. **RMSE**：与观测数据的均方根误差（仅在重叠时间段）
3. **AUC@T**：前T天的曲线下面积
4. **T80/T90/T95**：达到80%/90%/95%完成的天数

### 6.2 鲁棒性指标

```python
ROBUSTNESS_METRICS = {
    "performance_stability": "跨场景性能方差的倒数",
    "worst_case_ratio": "最差性能/平均基线性能",
    "adaptation_speed": "条件变化后的性能恢复",
    "failure_rate": "灾难性失败的场景比例"
}
```

### 6.3 泛化测试

- 在Mataram训练
- 在其他6个地区测试
- 测量跨地区的性能保持率

## 七、关键设计决策

### 7.1 为什么M=512？

```python
M_CANDIDATES = 512  # 从256增加到512
```

- 平衡资源利用率（中期40%）与计算可行性（2054维观察）
- 反映现代调度员配合计算机辅助的认知能力
- 在{96, 256, 384, 512}的消融研究中表现最佳

### 7.2 为什么纯随机选择？

之前尝试过的方案（已废弃）：
```python
# 错误方案：预定义策略偏见
"mixed_40_40_20": {
    "ljf_ratio": 0.4,
    "sjf_ratio": 0.4,
    "random_ratio": 0.2,
}
```

现在的方案：
```python
# 正确方案：纯随机，无偏见
candidates = random.choice(queue, size=M, replace=False)
```

**理由**：
1. 更诚实：不假设我们知道如何预筛选
2. 更自然：真实世界信息获取是随机的
3. 更强测试：RL必须处理任何出现的候选
4. 真正鲁棒：当无法控制输入质量时的表现

### 7.3 为什么用day-based seed？

```python
selection_seed = self.seed + self.day * 1000
```

确保**信息公平**：
- 同一天，所有方法（RL/LJF/SJF）看到完全相同的候选
- 不同方法的性能差异纯粹来自决策质量，而非信息差异
- 这是公平比较的关键

## 八、运行验证

### 8.1 诊断测试（Cell 4 in main.ipynb）

运行后应看到：
```
📉 LJF Ordering (should be DESCENDING by total work):
    0: ID=1047, Total=160 days
    1: ID=896,  Total=149 days
    2: ID=977,  Total=126 days
    ...

📈 SJF Ordering (should be ASCENDING by total work):
    0: ID=888,  Total=44 days
    1: ID=872,  Total=72 days
    2: ID=1024, Total=82 days
    ...
```

### 8.2 系统验证

运行 `python test_overall_system.py` 应看到：
```
✅ ALL SYSTEMS FUNCTIONAL

Key mechanisms verified:
  ✓ Batch arrival system
  ✓ Capacity ramp system
  ✓ Pure random candidate selection
  ✓ Policy orderings
```

### 8.3 关键检查点

1. **候选选择的随机性**
   - 查看Cell 4的输出
   - 候选ID应该看起来随机，不是按任何顺序

2. **LJF/SJF排序正确性**
   - LJF：工作量递减
   - SJF：工作量递增

3. **批次到达**
   - Day 0: ~40%房屋
   - Day 21: +35%房屋
   - Day 36: +25%房屋

4. **容量爬坡**
   - Day 0-35: K=0
   - Day 36-215: K逐渐增加
   - Day 216+: K=最大值

## 九、预期结果

### 9.1 性能模式

根据资源可用性：
- **高资源（1.0）**：LJF表现好，SJF表现差
- **低资源（0.1）**：SJF表现好，LJF表现差
- **RL**：在所有条件下保持稳定，"永远不太差"

### 9.2 信息成本

Oracle（无M限制）vs 常规（M=512）：
- 预期约165%的makespan增加
- 这量化了认知限制的成本

### 9.3 鲁棒性证明

RL的成功标准：
1. 没有灾难性失败模式
2. 最差情况优于基线的最差情况
3. 跨地区泛化性能下降<20%

## 十、总结

本系统的核心创新：

1. **纯随机候选选择**：无偏见的真实测试
2. **信息公平原则**：day-based seed确保公平比较
3. **批次到达+容量爬坡**：模拟真实世界约束
4. **鲁棒性优于最优性**：关注"永远不太差"而非"有时最好"

这不是一个优化问题，而是一个**鲁棒性和泛化**问题。RL的价值在于学习自适应策略，能够识别当前情况并选择合适的行为模式，而不是盲目遵循固定规则。

通过这种设计，我们能够诚实地测试RL是否真的学到了有用的东西，而不是仅仅利用了环境设计的漏洞。